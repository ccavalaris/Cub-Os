# Postgres. Local dev or a hosted Supabase/Neon connection string.
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/context_engine?schema=public"

# Optional. With a key, note extraction and Ask the Course run on Claude.
# Without one, both fall back to a deterministic parser and the UI says so.
# ANTHROPIC_API_KEY="sk-ant-..."
